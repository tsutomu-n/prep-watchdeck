{
  "name": "prep-watchdeck-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "prepare": "svelte-kit sync || true",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "generate:types": "json2ts --maxItems=-1 -i ../../schemas/universe-snapshot.schema.json -o src/lib/generated/universe-snapshot.d.ts && json2ts --maxItems=-1 -i ../../schemas/market-chart.schema.json -o src/lib/generated/market-chart.d.ts && json2ts --maxItems=-1 -i ../../schemas/selected-market.schema.json -o src/lib/generated/selected-market.d.ts && json2ts --maxItems=-1 -i ../../schemas/service-state.schema.json -o src/lib/generated/service-state.d.ts"
  },
  "dependencies": {
    "@sveltejs/adapter-auto": "^6.1.0",
    "@sveltejs/kit": "^2.55.0",
    "ajv": "^8.17.1",
    "lightweight-charts": "^5.2.0",
    "svelte": "^5.54.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@types/node": "^24.10.2",
    "json-schema-to-typescript": "^15.0.4",
    "svelte-check": "^4.3.4",
    "typescript": "^5.9.3",
    "vite": "^8.0.16",
    "vitest": "^4.0.15"
  }
}
